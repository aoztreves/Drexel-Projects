This is a readme file for Aslan Oztreves's assingment one Problems. 
1 through 4.


Every problem is commented out so there shouldn't be any problem.

Program 1 is used to find the total number of messages sent and the number
of directories

Program 2 is used to find the problem has the longest thread and list the
threads that are that of length.

Program 3 is used to find the emails sent in a certain time, which we get
that time from user input.

Program 4 is used to find the host names of the people who sent DR email as
well as how many emails they sent.
